cmake_minimum_required(VERSION 3.10)
project(netcat VERSION 1.10)
add_executable(nc doexec.c generic.h getopt.c getopt.h netcat.c)
option(E_OPTION "-e option (dangerous!!)" OFF)
if(E_OPTION)
  add_definitions(-DGAPING_SECURITY_HOLE)
endif()
if(WIN32)
  target_link_libraries(nc ws2_32)
endif()